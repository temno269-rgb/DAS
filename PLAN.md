1. Что находится в исходном H5

Основной набор данных — матрица:

x[i,c]

где:

i — номер временного отсчёта;
c — номер канала вдоль кабеля;
x — записанное оборудованием значение фазы или её изменения;
при частоте 1000 Гц между отсчётами проходит 0,001 секунды;
соседние каналы расположены примерно через 1,6 метра.

Условно:

                  каналы кабеля
время        c0       c1       c2       c3
0,000 с     x00      x01      x02      x03
0,001 с     x10      x11      x12      x13
0,002 с     x20      x21      x22      x23
...

Важно: пока нет физической калибровки оборудования, это именно значения фазы в единицах прибора. Их нельзя честно называть звуковым давлением, деформацией или децибелами.

2. Чтение исходных значений

Сначала адаптер:

Находит в H5 набор Phase.
Определяет, где расположена временная ось, а где каналы.
Читает частоту, время начала записи и шаг между каналами.
Если числа записаны как упакованные 16-битные значения, расшифровывает их.
Переводит считанный блок в 32-битные числа.

Никакие фильтры не применяются:

пики не вырезаются;
среднее не вычитается;
полосы частот не ограничиваются;
фон из сигнала не удаляется;
пропуски не заменяются выдуманными значениями.

Файл читается блоками примерно по 128–256 МБ. Например:

прочитать блок
→ обработать
→ записать компактный результат
→ освободить память
→ прочитать следующий блок

Это позволяет обработать 5 ГБ, не загружая все 5 ГБ в оперативную память.

3. Две независимые ветви обработки

После чтения работа разделяется на две ветви.

Phase
├── временная ветвь
│   ├── водопад
│   └── активность
│
└── частотная ветвь
    ├── спектрограмма
    ├── спектральная плотность мощности
    └── мощность частотных полос

Я бы выполнял их двумя последовательными проходами по файлу:

Первый проход — временные характеристики.
Второй проход — частотные характеристики.

Это немного увеличивает время первичной обработки, но делает код значительно понятнее и не создаёт конкуренцию за диск и видеопамять. Для нашей задачи это приемлемо.

Временная ветвь
4. Базовые окна по 0,25 секунды

При частоте 1000 Гц одно окно содержит:

N=0,25⋅1000=250

исходных отсчётов.

Для каждого канала отдельно берутся 250 значений:

x
0
	​

,x
1
	​

,…,x
249
	​


и один раз рассчитываются общие характеристики:

sum=∑x
i
	​

sumsq=∑x
i
2
	​

mean=
count
sum
	​

rms=
count
sumsq
	​

	​

min=min(x
i
	​

)
max=max(x
i
	​

)
peak-to-peak=max−min

Также сохраняются:

число действительных значений;
число пропущенных значений;
номер отсчёта положительного максимума;
номер отсчёта отрицательного минимума;
канал каждого экстремума.

Это принципиально важно: нельзя оставлять только один «главный пик». В одном окне одновременно могут существовать сильный положительный и сильный отрицательный всплески. Поэтому сохраняются оба.

Что здесь теряется

Полная форма 250 отсчётов больше не сохраняется. Например, мы уже не знаем порядок всех небольших колебаний внутри окна.

Но сохраняются:

точная амплитуда максимума;
точная амплитуда минимума;
их положение;
среднее;
среднеквадратичное значение;
суммарная энергия.

Для шумомера и поиска отклонений это разумный компромисс.

5. Водопад

Водопад — это временно-пространственная карта:

ось Y → время
ось X → положение вдоль кабеля
цвет → выбранная характеристика

Он не выполняет собственный анализ. Он получает уже рассчитанные статистики окон по 0,25 секунды.

Для каждой ячейки можно получить:

среднеквадратичное значение;
минимум;
максимум;
размах между минимумом и максимумом;
положительный и отрицательный пики;
координаты пиков.

Для гарантированного отображения всплесков браузер должен получать пару:

min + max

а не только среднее или один пик. Для линейного временного графика это рисуется как диапазон между минимумом и максимумом.

6. Карта активности

Карта активности получает те же самые окна. Исходный H5 повторно ради неё не читается.

Сначала берётся фон конкретного канала за предыдущие 60 секунд:

μ
фон
	​

=
count
предыдущие
	​

∑x
предыдущие
	​

	​

σ
фон
	​

=
count
предыдущие
	​

∑x
предыдущие
2
	​

	​

−μ
фон
2
	​

	​


Затем рассчитываются отклонения текущего окна:

D
+
=max(0,max−μ
фон
	​

)
D
−
=max(0,μ
фон
	​

−min)

Нормированное отклонение:

S=
σ
фон
	​

+ε
max(D
+
,D
−
)
	​


В результате сохраняются:

среднеквадратичная активность;
среднее;
положительное отклонение;
отрицательное отклонение;
нормированная величина отклонения;
время и канал экстремума.

Фон используется только для оценки необычности события. Он не вычитается из исходного сигнала и не меняет сохранённые минимумы и максимумы.

Важное исправление

Фон должен переходить между соседними файлами одной непрерывной серии. Если каждый 200-мегабайтный файл начинает считать фон заново, первые 60 секунд каждого файла будут некорректными.

При реальном временном разрыве фон, наоборот, должен сбрасываться.

Многоуровневая временная структура
7. Как строятся более крупные интервалы

После уровня 0,25 секунды исходные данные повторно не читаются.

Из маленьких ячеек строятся более крупные:

0,25 с
→ 1 с
→ 5 с
→ 30 с
→ 1 мин
→ 5 мин
→ 1 час
→ 6 часов
→ сутки
→ весь файл

Объединение выполняется так:

sum
P
	​

=∑sum
C
	​

sumsq
P
	​

=∑sumsq
C
	​

count
P
	​

=∑count
C
	​

min
P
	​

=min(min
C
	​

)
max
P
	​

=max(max
C
	​

)

Индекс экстремума переносится из той дочерней ячейки, где он реально находился.

Среднеквадратичное значение родителя рассчитывается из сумм:

rms
P
	​

=
count
P
	​

sumsq
P
	​

	​

	​


Нельзя просто усреднять дочерние среднеквадратичные значения — это математически некорректно.

Такая же пирамида строится по кабелю:

1 канал
→ 2 канала
→ 4
→ 8
→ 16
→ 32
→ 64
→ ...
→ весь участок

Все каналы участвуют в объединении. Выбор каждого второго или каждого десятого канала запрещён.

Частотная ветвь
8. Подготовка спектральных кадров

Частотная ветвь снова читает исходную фазу блоками.

Для каждого канала берётся окно длиной 1 секунда:

N=1000

Следующее окно начинается через 0,5 секунды:

кадр 1: 0,0–1,0 с
кадр 2: 0,5–1,5 с
кадр 3: 1,0–2,0 с

Перед частотным преобразованием значения умножаются на окно Ханна:

w[n]=0,5−0,5cos(
N
2πn
	​

)

Оно необходимо, чтобы границы секундного фрагмента не создавали искусственные частоты.

Затем выполняется преобразование:

F[k,c]=FFT(x[n,c]⋅w[n])

После него получается комплексное представление амплитуды и фазы каждой частоты. Для наших графиков сохраняется мощность:

P[k,c]=
f
s
	​

∑w[n]
2
∣F[k,c]∣
2
	​


При 1000 Гц получается 501 частота:

0, 1, 2, ... 500 Гц
Что здесь необратимо теряется

После перехода к мощности теряется спектральная фаза. По сохранённой мощности нельзя восстановить исходную временную форму сигнала.

Для спектрограммы, спектральной плотности и мощности полос это нормально: этим графикам нужна именно мощность частот.

Но это должно быть явно записано как допустимая потеря.

9. Спектрограмма

Спектрограмма показывает:

ось X → время
ось Y → частота
цвет → мощность

Из общих спектральных кадров для каждой частоты сохраняются:

сумма мощности;
средняя мощность;
максимальная мощность;
число вошедших кадров и каналов;
время и канал максимума.

Для обнаружения коротких всплесков основной отображаемой величиной должна быть максимальная мощность. Средняя мощность может сильно ослабить короткое событие.

Проблема группировки восьми каналов

Текущий TODO предлагает объединять по 8 соседних каналов. Тогда:

все восемь входят в сумму;
сильнейший сохраняется как максимум;
сохраняется канал сильнейшего;
но спектры остальных отдельных каналов восстановить уже нельзя.

Здесь необходимо принять явное ограничение.

Оптимальный компромисс:

спектрограмма хранится группами по 8 каналов;
сумма, максимум и координаты максимума сохраняются;
мощность частотных полос хранится отдельно для каждого канала;
интерфейс не обещает полный спектр одного канала после такой группировки.

Если полный спектр любого отдельного канала обязателен, базовая группа должна быть равна одному каналу. Но размер результата тогда может стать сопоставимым с исходным H5 или даже превысить его.

10. Спектральная плотность мощности

Отдельное частотное преобразование здесь не выполняется.

Используются те же мощности, которые были рассчитаны для спектрограммы:

PSD[k]=
количество наблюдений
∑P[k]
	​


Для выбранного времени и участка кабеля складываются готовые спектральные ячейки.

Дополнительно рассчитываются:

преобладающая частота;
мощность преобладающей частоты;
центр спектра;
ширина спектра;
спектральная энтропия;
частота, ниже которой находится 95% мощности.

Эти показатели можно заранее сохранять для каждого уровня. При изменении ползунков новая обработка исходного сигнала не требуется.

Фактически спектрограмма и спектральная плотность используют одну и ту же основу. Поэтому отдельный psd.h5, полностью дублирующий спектральные суммы, не нужен.

Лучше:

spectral.h5
├── спектральная пирамида
├── готовые характеристики спектра
└── мощности частотных полос
11. Мощность частотных полос

Для каждого спектрального кадра и канала мощность суммируется внутри диапазонов:

0–20 Гц
20–80 Гц
80–250 Гц
250–500 Гц

Математически:

E
B
	​

[c]=
k
∑
	​

P[k,c]Δf
k
	​


где частотная ячейка пересекается с полосой B.

Результат хранится для каждого канала с шагом спектральных кадров 0,5 секунды:

средняя мощность полосы;
максимальная мощность;
время максимума;
канал максимума.

Затем из этих значений строятся старшие временные и пространственные уровни.

Хранение результата
12. Что в итоге записывается

Я бы изменил предложенные пять файлов на два основных:

calculated_data/
└── series/
    └── <series_id>/
        └── <file_id>/
            ├── manifest.json
            ├── temporal.h5
            └── spectral.h5

temporal.h5:

базовые статистики по 0,25 с
водопад
активность
временные и пространственные уровни
min/max и координаты пиков

spectral.h5:

общие спектральные мощности
спектрограмма
спектральная плотность
мощности полос
временные и пространственные уровни
максимумы и координаты

Это не означает объединение всей программы в один монолит. Модули расчёта остаются отдельными, но общие результаты физически не дублируются в пяти файлах.

Исходный H5 остаётся первичным источником. Новый файл содержит только подготовленные данные для отображения.

Работа веб-интерфейса
13. Выбор участка кабеля

Внутренне ползунок хранит не метры, а номера каналов:

начальный канал = 100
конечный канал = 400

Метры рассчитываются только для подписи:

d=c\cdot\text{meters_per_channel}

Если шаг равен 1,6 м, ползунок физически не может остановиться между каналами.

14. Выбор времени

Ползунок движется с шагом 1 секунда. Это только ограничение интерфейса.

Внутри выбранного интервала остаются:

временные ячейки по 0,25 секунды;
спектральные кадры с шагом 0,5 секунды;
исходные координаты пиков с точностью одного отсчёта, то есть 0,001 секунды.

Пока пользователь двигает ползунки, запрос не выполняется. Он нажимает «Применить», после чего интерфейс отправляет один запрос с готовыми границами.

15. Что делает сервер после «Применить»

Сервер:

Получает время, каналы и размеры графика.
Выбирает подходящий готовый уровень.
Читает только пересекающиеся блоки temporal.h5 и spectral.h5.
Возвращает подготовленные числовые массивы.
Браузер обновляет графики.

Сервер при этом не должен:

запускать задачу обработки;
читать исходный Phase;
выполнять частотное преобразование;
пересчитывать активность;
пересчитывать мощность полос.

Допускается только быстрое сложение небольшого количества готовых ячеек на границах выбранного диапазона.

16. Обработка файлов и построение серий
16.1. Единица обработки

Одна задача всегда обрабатывает ровно один физический H5-файл.

Если файл меньше 256 МБ — он обычно читается одним блоком.
Если файл больше — читается последовательными блоками не более 256 МБ.
Один вычислительный блок никогда не содержит данные двух физических файлов.
Граница блока внутри файла не является границей математического окна: незавершённые 0,25-секундные окна и хвосты частотных кадров переносятся в следующий блок.

То есть:

200 МБ → один блок обработки
5 ГБ → много блоков до 256 МБ

Но результат в обоих случаях относится к одному физическому файлу.

При записи нельзя просто добавлять данные «в конец». Каждый рассчитанный блок записывается в заранее определённый диапазон:

temporal.h5[time_start:time_end]
spectral.h5[frame_start:frame_end]

В паспорте сохраняются завершённые диапазоны. После сбоя обработка может продолжиться с первого незавершённого диапазона.

16.2. Каталог результатов и обнаружение файлов

Единственная нормативная структура calculated_data и полный порядок работы наблюдателя описаны в разделе «WATCHER PRINCIPE» ниже. Другие варианты структуры каталогов не используются.

Наблюдатель создаёт начальный manifest.json до постановки файла в очередь. ProcessFileJob создаёт temporal.h5 и spectral.h5 в каталоге этого физического файла и обновляет состояние и пути в manifest.json.

Если исходный файл позднее изменился, старая обработка инвалидируется.

17. Серия и непрерывный участок

Серия означает, что файлы описывают одинаковую конфигурацию:

одинаковая частота;
одинаковое количество каналов;
одинаковый шаг между каналами;
одинаковое начало и окончание участка кабеля;
одинаковый порядок осей;
одинаковый тип и единица сигнала.

Однако внутри одной серии могут находиться несколько непрерывных временных участков:

Файлы относятся к одной серии, но между участками находится полуторачасовой разрыв.

Для соседних файлов вычисляется:

t
ожидаемое
	​

=t
начала предыдущего
	​

+
f
s
	​

samples предыдущего
	​

gap=t
начала следующего
	​

−t
ожидаемое
	​


Дальше:

gap > 1 секунды — временной разрыв;
0 ≤ gap ≤ 1 секунды — непрерывное продолжение;
gap < 0 — временное наложение.

На этом этапе наблюдатель только фиксирует временное отношение файлов. Он не определяет дубликат или конфликт и не сравнивает значения пересекающихся файлов. Обработка наложений выполняется позднее по правилам раздела 21.

Перекрывающиеся файлы нельзя усреднять или молча накладывать.
17.5. Нормативные правила выбора файлов в веб-интерфейсе

Этот раздел является основным контрактом выбора файлов. Наблюдатель, сортировка серии, manifest.json, FastAPI и веб-интерфейс должны использовать одинаковые правила.

FastAPI формирует список серий и файлов из manifest.json каждого обработанного файла. SQLite используется как быстрый индекс путей и порядка, но значения, показываемые пользователю, берутся из manifest.json.

Для каждого файла интерфейс получает из manifest.json:

технический идентификатор файла и серии;
исходное имя файла;
начало, окончание и продолжительность записи;
начало и окончание участка кабеля;
частоту, число каналов и шаг между каналами;
состояние обработки;
идентификатор непрерывного участка;
отношение к предыдущему файлу;
точную величину разрыва или границы наложения;
признак duplicate или conflict после выполнения проверки наложения.

Пользователь самостоятельно отмечает файлы внутри выбранной серии. Разрешено совместно применять любое количество стыкующихся файлов, файлов с допустимым разрывом не более одной секунды и перекрывающихся файлов одного непрерывного участка.

Правила проверки выбора:

если 0 ≤ Δt ≤ 1 секунды, файлы считаются одним непрерывным участком; точная величина отсутствующего интервала показывается пользователю;
если Δt < 0, совместный выбор разрешён, но интерфейс показывает предупреждение о наложении и выбранном источнике;
если Δt > 1 секунды, файлы относятся к разным непрерывным участкам и совместное отображение запрещено;
файлы разных серий нельзя включать в один запрос отображения.

Интерфейс не должен молча изменять выбор пользователя. При недопустимой комбинации он подсвечивает границу разрыва, объясняет причину и не позволяет применить выбор, пока пользователь не снимет файлы одного из непрерывных участков.

В начале страницы выводится список серий. Для каждой серии показываются участок кабеля и общий временной диапазон крайних файлов, затем непрерывные участки и входящие в них физические файлы.

18. Границы соседних файлов

Порог в одну секунду из раздела 17.5 управляет совместным выбором в интерфейсе, но не разрешает искусственно заполнять отсутствующие исходные отсчёты.

Пограничный частотный кадр можно строить только при математически непрерывных файлах: время начала следующего файла совпадает с ожидаемым временем продолжения предыдущего с погрешностью не более одного исходного отсчёта.

Например, только при такой непрерывности:

file_001: последние 0,5 с
file_002: первые 0,5 с

могут составлять одно полноценное секундное окно.

Основная обработка остаётся строго по одному файлу, а после подтверждения математической непрерывности выполняется небольшая обработка границы:

для частотной ветви читается только необходимый хвост и начало соседнего файла;
для активности предыдущие 60 секунд берутся из уже готовых временных статистик;
пограничный результат записывается только одному файлу по правилу положения центра кадра;
дублирование кадра в двух артефактах запрещено.

Если 0 < Δt ≤ 1 секунды, совместный выбор разрешён, но отсутствующие значения не создаются, частотный кадр через границу не строится, фон активности сбрасывается, а точный разрыв показывается пользователю.

Если Δt > 1 секунды, действуют те же математические правила, но файлы дополнительно относятся к разным непрерывным участкам и не могут быть выбраны совместно.
19. Объединение для веб-интерфейса

Физически склеивать все рассчитанные файлы в один гигантский H5 необязательно.

manifest.json является постоянным источником паспортных данных и временных отношений, которые FastAPI передаёт веб-интерфейсу. SQLite хранит индекс этих паспортов:

путь к manifest.json;
порядок файлов;
абсолютные времена;
идентификаторы непрерывных участков;
разрывы;
перекрытия;
пути к готовым артефактам.

После нажатия «Применить» сервер:

Проверяет выбор по нормативным правилам раздела 17.5.
Определяет, какие файлы пересекают выбранное время.
Читает нужные диапазоны из их готовых temporal.h5 и spectral.h5.
Расставляет данные по абсолютному времени.
Если выбор пересекает разрыв больше одной секунды, отклоняет запрос и возвращает описание границы из manifest.json.

Отдельные series_temporal.h5 и series_spectral.h5 не создаются. Сервер читает готовые уровни из файловых temporal.h5 и spectral.h5, используя порядок и границы из паспортов, проиндексированных в SQLite.

20. Представление в интерфейсе

Интерфейс строится иерархически:

Серия
→ непрерывный участок
→ физические файлы

Состав отображаемых полей, допустимые комбинации файлов, уведомления и ограничения определены разделом 17.5. Интерфейс получает эти сведения через FastAPI из manifest.json и не рассчитывает временные отношения самостоятельно.

21. Обработка временных наложений файлов

Если два совместимых файла одной серии пересекаются по времени, применяется следующий порядок:

По паспортам вычислить точный интервал наложения:

overlap
start
	​

=max(start
1
	​

,start
2
	​

)
overlap
end
	​

=min(end
1
	​

,end
2
	​

)
Оба физических файла обработать полностью. Не обрезать исходные данные и не пропускать вычисления второго файла.
Для пересекающегося диапазона вычислить контрольные суммы исходных отсчётов или блоков.
Если значения полностью совпадают, пометить наложение как duplicate. В общей временной шкале использовать только один экземпляр.
Если значения отличаются, пометить участок как conflict. Автоматически усреднять, складывать или смешивать результаты запрещено.
По умолчанию владельцем конфликтного временного участка считать файл, который начался раньше. Пользователь должен иметь возможность переключиться на второй файл.

Оба рассчитанных результата сохранять полностью:

file_001/temporal.h5
file_001/spectral.h5

file_002/temporal.h5
file_002/spectral.h5
После проверки обновить manifest.json обоих файлов и индекс SQLite. Сохранить:
оба идентификатора файлов;
начало и конец наложения;
тип duplicate или conflict;
выбранный по умолчанию файл;
контрольные суммы пересекающихся блоков.

FastAPI передаёт эти сведения интерфейсу из обновлённых manifest.json.
При построении общих уровней серии учитывать конфликтный диапазон только один раз, используя выбранного владельца.
Частотные кадры и окна активности рассчитывать для обоих файлов, но в канонический вид серии включать результаты только выбранного владельца.
FastAPI должен возвращать вместе с данными описание наложения и использованный источник.
В интерфейсе показывать предупреждение:

Файлы пересекаются по времени. Значения отличаются. Сейчас отображается file_001.h5.

Переключение источника в интерфейсе должно только менять читаемый готовый артефакт. Повторная обработка исходного H5 не запускается.



WATCHER PRINCIPE:
1. Назначение наблюдателя

Наблюдатель отвечает только за:

обнаружение H5
→ проверку завершённости копирования
→ чтение паспорта
→ создание технического идентификатора
→ поиск или создание серии
→ сортировку файлов серии
→ определение временного положения
→ запись связей с соседними файлами в manifest.json
→ создание каталогов
→ постановку файла в очередь

Наблюдатель не должен:

рассчитывать графики;
сравнивать значения пересекающихся файлов;
определять, является ли наложение дубликатом;
обрезать файлы;
объединять исходные массивы.

Наложение сравнивается позднее, после готовности рассчитанных результатов обоих файлов. Результат проверки записывается в их manifest.json до передачи сведений веб-интерфейсу.

2. Обнаружение нового файла

Наблюдатель реагирует на появление .h5 и дополнительно пересматривает входную папку примерно каждые 5 секунд.

Файл считается готовым, если:

его размер не изменился за две последовательные проверки;
время изменения не поменялось;
файл успешно открывается;
набор Phase найден;
форма и обязательные метаданные читаются корректно.

Состояния:

discovered
→ stabilizing
→ inspected
→ indexed
→ queued
→ processing
→ ready / failed

Папка calculated_data и временные файлы обязательно исключаются из наблюдения.

3. Паспорт физического файла

Из H5 извлекаются:

исходное имя и путь;
размер;
контрольная сумма;
время начала;
вычисленное время окончания;
частота;
число отсчётов;
число каналов;
шаг между каналами;
начало и окончание участка кабеля;
порядок осей;
тип данных;
идентификатор оборудования или линии, если он присутствует.

Время окончания рассчитывается как:

t
end
	​

=t
start
	​

+
f
s
	​

samples
	​


Внутренне окончание считается невключённой границей.

После сопоставления с серией наблюдатель дополняет manifest.json полями, необходимыми для выбора по разделу 17.5:

file_id;
series_id;
continuous_segment_id;
processing_status;
previous_file_id;
relation_to_previous: first, continuous, gap или overlap;
delta_t_seconds;
overlap_start и overlap_end для наложения.

Поля duplicate, conflict и default_source наблюдатель не вычисляет. Их добавляет поздняя проверка наложения после обработки обоих файлов.

4. Названия серий и файлов

Время не входит в имя каталога. Оно хранится только в паспорте и SQLite.

Техническое имя серии:

series_0-5000m_a84f19c203bd

Техническое имя результата файла:

file_0-5000m_a23b19e4

Здесь:

0-5000m — участок кабеля;
последние символы — короткий технический идентификатор;
полная контрольная сумма хранится в паспорте.

Метраж в имени нужен для навигации, но истинным идентификатором остаётся контрольная сумма. Поэтому одинаковые участки разных серий или разные файлы одной серии не конфликтуют по именам.

Отображаемое название серии:

Кабель 0–5000 м · 1000 Гц · 3125 каналов

Пользователь может изменить отображаемое название без переименования технической директории.

5. Сопоставление файла с серией

Файл относится к существующей серии, если совпадают:

идентификатор линии или оборудования, если он существует;
начало и окончание участка кабеля;
количество каналов;
шаг между каналами;
частота;
порядок осей;
физический тип данных.

Время записи не участвует в создании серии. Поэтому одна серия может содержать записи одного кабеля за разные часы или дни.

Если идентификатора оборудования нет, для защиты от случайного объединения дополнительно учитывается исходная директория эксперимента.

6. Определение временного отношения файлов

После добавления нового файла все файлы серии сортируются по времени начала.

Для каждой соседней пары:

Δt=t
start следующего
	​

−t
end предыдущего
	​


Классификация:

Δt>1 секунды — временной разрыв;
0≤Δt≤1 секунды — непрерывное продолжение;
Δt<0 — временное наложение.

Наложение входит в тот же непрерывный участок, но его интервал отдельно фиксируется:

relation: overlap
overlap_start
overlap_end

Наблюдатель не определяет:

duplicate
conflict

Он только сообщает, что два файла пересекаются. Сравнение рассчитанных значений выполняется позднее, после обработки обоих файлов.

После сортировки наблюдатель записывает в manifest.json каждого файла идентификатор непрерывного участка и отношение к предыдущему файлу. Если новый файл вставлен между уже известными файлами, manifest.json нового файла и затронутого следующего файла обновляются, после чего индекс SQLite синхронизируется с ними.

Даже если разрыв меньше секунды и считается допустимым продолжением, его точная длительность сохраняется в manifest.json и показывается пользователю как уведомление.

7. Создание структуры и передача дальше

Результирующая структура:

calculated_data/
├── index.sqlite
└── series/
    ├── series_0-5000m_a84f19c203bd/
    │    ├── file_0-5000m_a23b19e4/
    │    │   ├── manifest.json
    │    │   ├── temporal.h5
    │    │   └── spectral.h5
    │    └── file_0-5000m_b31c287d/
    │        ├── manifest.json
    │        ├── temporal.h5
    │        └── spectral.h5
    │
    └── series_0-2000m_a84f19c203bd/
         ├── file_0-2000m_a23b19e4/
         │   ├── manifest.json
         │   ├── temporal.h5
         │   └── spectral.h5
         └── file_0-2000m_b31c287d/
             ├── manifest.json
             ├── temporal.h5
             └── spectral.h5


После создания паспорта наблюдатель:

Создаёт директорию результата файла.
Записывает начальный manifest.json.
Создаёт или обновляет серию и порядок файлов.
Записывает в manifest.json временные отношения и идентификатор непрерывного участка.
Обновляет manifest.json затронутого соседнего файла, если его связь изменилась.
Синхронизирует индекс SQLite с паспортами.
Передаёт идентификатор файла в очередь обработки.

После завершения ProcessFileJob состояние обработки и пути к готовым temporal.h5 и spectral.h5 записываются в manifest.json. Только после этого файл помечается готовым для выбора в интерфейсе.
