1. Что находится в исходном H5

Основной набор данных — матрица:

x[i,c]

где:

i — номер временного отсчёта;
c — номер канала вдоль кабеля;
x — записанное оборудованием значение фазы или её изменения;
при частоте 1000 Гц между отсчётами проходит 0,001 секунды;
соседние каналы расположены примерно через 1,6 метра.

Условно:

                  каналы кабеля
время        c0       c1       c2       c3
0,000 с     x00      x01      x02      x03
0,001 с     x10      x11      x12      x13
0,002 с     x20      x21      x22      x23
...

Важно: пока нет физической калибровки оборудования, это именно значения фазы в единицах прибора. Их нельзя честно называть звуковым давлением, деформацией или децибелами.

2. Чтение исходных значений

Сначала адаптер:

Находит в H5 набор Phase.
Определяет, где расположена временная ось, а где каналы.
Читает частоту, время начала записи и шаг между каналами.
Если числа записаны как упакованные 16-битные значения, расшифровывает их.
Переводит считанный блок в 32-битные числа.

Никакие фильтры не применяются:

пики не вырезаются;
среднее не вычитается;
полосы частот не ограничиваются;
фон из сигнала не удаляется;
пропуски не заменяются выдуманными значениями.

Файл читается блоками, содержащими не более 256 МиБ уже расшифрованной Phase в float32. Размер исходного H5 на диске, его сжатие и исходный 16-битный тип при выборе блока не используются.

Например:

прочитать срез одного физического файла
→ расшифровать не более 256 МиБ Phase
→ обработать
→ записать компактный результат
→ освободить блок
→ прочитать следующий срез того же файла

Точная формула, ограничения промежуточной памяти и перенос хвостов описаны в разделе 16.1.

3. Две независимые ветви обработки

После чтения работа разделяется на две ветви.

Phase
├── временная ветвь
│   ├── водопад
│   └── активность
│
└── частотная ветвь
    ├── спектрограмма
    ├── спектральная плотность мощности
    └── мощность частотных полос

Я бы выполнял их двумя последовательными проходами по файлу:

Первый проход — временные характеристики.
Второй проход — частотные характеристики.

Это немного увеличивает время первичной обработки, но делает код значительно понятнее и не создаёт конкуренцию за диск и видеопамять. Для нашей задачи это приемлемо.

Временная ветвь
4. Базовые окна по 0,25 секунды

При частоте 1000 Гц одно окно содержит:

N=0,25⋅1000=250

исходных отсчётов.

Для каждого канала отдельно берутся 250 значений:

x
0
	​

,x
1
	​

,…,x
249
	​


и один раз рассчитываются общие характеристики:

sum=∑x
i
	​

sumsq=∑x
i
2
	​

mean=
count
sum
	​

rms=
count
sumsq
	​

	​

min=min(x
i
	​

)
max=max(x
i
	​

)
peak-to-peak=max−min

Также сохраняются:

число действительных значений;
число пропущенных значений;
номер отсчёта положительного максимума;
номер отсчёта отрицательного минимума;
канал каждого экстремума.

Достаточные статистики и координаты записываются в temporal.h5:/levels/<time_level>/<space_level>/statistics/.

Это принципиально важно: нельзя оставлять только один «главный пик». В одном окне одновременно могут существовать сильный положительный и сильный отрицательный всплески. Поэтому сохраняются оба.

Что здесь теряется

Полная форма 250 отсчётов больше не сохраняется. Например, мы уже не знаем порядок всех небольших колебаний внутри окна.

Но сохраняются:

точная амплитуда максимума;
точная амплитуда минимума;
их положение;
среднее;
среднеквадратичное значение;
суммарная энергия.

Для шумомера и поиска отклонений это разумный компромисс.

5. Водопад

Водопад — это временно-пространственная карта:

ось Y → время
ось X → положение вдоль кабеля
цвет → выбранная характеристика

Он не выполняет собственный анализ. Он получает уже рассчитанные статистики окон по 0,25 секунды.

Для каждой ячейки можно получить:

среднеквадратичное значение;
минимум;
максимум;
размах между минимумом и максимумом;
положительный и отрицательный пики;
координаты пиков.

Готовые показатели водопада записываются в temporal.h5:/levels/<time_level>/<space_level>/waterfall/.

Для гарантированного отображения всплесков браузер должен получать пару:

min + max

а не только среднее или один пик. Для линейного временного графика это рисуется как диапазон между минимумом и максимумом.

6. Карта активности

Карта активности использует достаточные статистики временных окон. Исходный H5 повторно ради неё не читается.

Для текущей ячейки A и того же пространственного диапазона G берётся строго предшествующий фон:

background_interval = [A.start - 60 секунд, A.start)

Текущая ячейка в фон не входит.

Из достаточных статистик фона:

background_count = sum(count_j)
background_sum = sum(sum_j)
background_sumsq = sum(sumsq_j)

Если background_count > 0:

background_mean = background_sum / background_count

background_variance = max(
    background_sumsq / background_count - background_mean²,
    0
)

background_std = sqrt(background_variance)

Для текущей ячейки:

mean = sum / count
rms_activity = sqrt(sumsq / count)
deviation_positive = max(0, max - background_mean)
deviation_negative = max(0, background_mean - min)

Нормированная оценка:

activity_epsilon = max(
    1e-12,
    1.1920929e-7 × max(1, abs(background_mean))
)

normalized_deviation =
    max(deviation_positive, deviation_negative)
    / (background_std + activity_epsilon)

Среднее перед этими расчётами из исходного сигнала не вычитается. Фон используется только как характеристика сравнения и не меняет sum, sumsq, min или max.

Координата экстремума выбирается так:

если deviation_positive > deviation_negative — координата max;
если deviation_negative > deviation_positive — координата min;
при равенстве выбирается более ранний отсчёт, затем меньший номер канала.

В temporal.h5:/levels/<time_level>/<space_level>/activity/ записываются:

background_count;
background_mean;
background_std;
rms_activity;
deviation_positive;
deviation_negative;
normalized_deviation;
extremum_value;
extremum_sample_index;
extremum_channel_index;
extremum_source_ref.

Если background_count=0, фон и показатели отклонения равны NaN, координаты равны -1, а строка получает состояние intentionally_missing. Если доступна только часть ожидаемой истории из-за ещё не готового известного соседа, результат получает provisional и позднее обновляется BoundaryFinalizeJob.

На подтверждённом начале непрерывного участка фон считается сброшенным. По мере появления реальной истории используются только существующие значения; отсутствующие отсчёты не заменяются нулями.

7. Построение старших временных и пространственных уровней

7.1. Достаточные статистики

После базового уровня исходный Phase повторно не читается. Для родительской ячейки P объединяются все дочерние ячейки C, полностью относящиеся к её временному и пространственному диапазону:

count_P = sum(count_C)
missing_count_P = sum(missing_count_C)
sum_P = sum(sum_C)
sumsq_P = sum(sumsq_C)
min_P = min(min_C)
max_P = max(max_C)

Координаты min и max переносятся из дочерней ячейки, в которой соответствующий экстремум реально находился.

Готовые значения родителя рассчитываются заново:

mean_P = sum_P / count_P
rms_P = sqrt(sumsq_P / count_P)
energy_P = sumsq_P
peak_to_peak_P = max_P - min_P

Усреднять дочерние mean, rms или peak_to_peak запрещено.

Временные уровни temporal.h5:

t_250ms
→ t_1s
→ t_5s
→ t_30s
→ t_60s
→ t_300s
→ t_3600s
→ t_21600s
→ t_86400s
→ t_file

Пространственные уровни:

c_1
→ c_2
→ c_4
→ c_8
→ c_16
→ c_32
→ c_64
→ ...
→ c_all

Все каналы участвуют в объединении. Выбор каждого второго или каждого десятого канала запрещён.

7.2. Activity на старших уровнях

Готовые background_mean, background_std, deviation_positive, deviation_negative и normalized_deviation дочерних ячеек никогда не усредняются и не суммируются.

Для каждой родительской ячейки Activity выполняется полный повторный расчёт:

объединить count, sum, sumsq, min и max по правилам 7.1;
рассчитать mean и rms_activity из объединённых sum, sumsq и count;
взять тот же пространственный диапазон родителя;
собрать достаточные статистики базового уровня t_250ms/c_1 за точный интервал [parent.start - 60 секунд, parent.start);
рассчитать background_count, background_mean и background_std;
заново рассчитать положительное, отрицательное и нормированное отклонение;
заново выбрать координату экстремума;
записать готовый результат в activity соответствующего уровня.

Даже если временная ячейка родителя равна одному часу, фоном остаются ровно реальные 60 секунд перед её началом. Использовать целиком предыдущую часовую ячейку вместо 60 секунд запрещено.

Для пространственного родителя фон объединяется по всем входящим каналам из их достаточных статистик. Выборка отдельных каналов или усреднение их готовых normalized_deviation запрещены.

Activity родительского уровня строится напрямую из statistics и точного фонового интервала. Группа activity дочернего уровня не является входом для математического расчёта родительской Activity.

Построение выполняется во время предварительной обработки. Если фоновый интервал пересекает границу файлов, затронутые ячейки сначала имеют provisional, а затем пересчитываются BoundaryFinalizeJob по разделу 18.

Частотная ветвь
8. Подготовка спектральных кадров

8.1. Размер и положение кадров

Частотная ветвь снова читает исходную Phase блоками, но каждый кадр рассчитывает отдельно для каждого канала.

При fs=1000 Гц:

N = fs × 1 секунда = 1000 отсчётов
H = fs × 0,5 секунды = 500 отсчётов

Кадр m начинается с отсчёта:

frame_start_sample = m × H

и содержит x[frame_start_sample + n, c] для n=0,...,N-1.

Первый кадр занимает 0,0–1,0 секунды, второй 0,5–1,5, третий 1,0–2,0.

Неполный кадр не рассчитывается, не дополняется нулями и не сохраняется. Если в секундном кадре конкретного канала имеется хотя бы одно недействительное значение, весь частотный кадр этого канала пропускается, а count для него не увеличивается.

8.2. Подготовка сигнала

Перед частотным преобразованием:

среднее не вычитается;
линейный тренд не удаляется;
фильтры не применяются;
амплитуда не нормируется;
используется периодическое окно Ханна.

Конкретное окно:

w[n] = 0,5 - 0,5 × cos(2πn / N),
n = 0,...,N-1

Знаменатель равен N, а не N-1. Использовать симметричный вариант окна Ханна запрещено.

Нормировка окна:

U = sum(w[n]²), n=0,...,N-1

8.3. Односторонняя спектральная плотность мощности

Для действительного сигнала используется только одностороннее преобразование rFFT:

X[k,c] = sum(
    x[n,c] × w[n] × exp(-i2πkn/N)
)

для k=0,...,N/2.

Сначала:

P_raw[k,c] = |X[k,c]|² / (fs × U)

Затем выполняется одностороннее масштабирование:

P[0,c] = P_raw[0,c]
P[N/2,c] = P_raw[N/2,c]
P[k,c] = 2 × P_raw[k,c], k=1,...,N/2-1

Нулевая частота и частота Найквиста не удваиваются. Все внутренние положительные частоты умножаются на два.

При fs=1000 Гц и N=1000:

F = N/2 + 1 = 501
frequency_hz[k] = k × fs/N = k Гц
k = 0,...,500

P имеет единицы «квадрат единицы прибора на герц». Частотное разрешение:

delta_f = fs/N = 1 Гц

Комплексные X и спектральная фаза являются промежуточными и после получения P не сохраняются.

8.4. Что сохраняется из каждого кадра

Полная частотная мощность P[k,c] отдельного кадра используется как промежуточный результат.

Из неё параллельно формируются:

вклад в базовую ячейку спектрограммы длительностью 2 секунды;
спектральная плотность и её характеристики на той же основе;
мощности полос для каждого отдельного канала с шагом 0,5 секунды.

Полная матрица P каждого кадра с уровнем t_500ms/c_1 в HDF5 не записывается. Это принципиальное ограничение размера результата.

В spectral.h5 сохраняются только линейные мощности. Логарифм и децибелы на этапе обработки не вычисляются.

9. Спектрограмма и её постоянные уровни

9.1. Базовая ячейка

Базовая временная ячейка спектрограммы имеет длительность 2 секунды.

Границы ячеек выравниваются по абсолютному времени UTC:

cell_start_ns =
    floor(frame_center_ns / 2_000_000_000)
    × 2_000_000_000

cell_end_ns = cell_start_ns + 2_000_000_000

В ячейку входят кадры, для которых:

cell_start_ns ≤ frame_center_ns < cell_end_ns

При шаге 0,5 секунды полная двухсекундная ячейка обычно содержит четыре положения центра кадра.

Базовая пространственная ячейка содержит восемь соседних каналов:

channel_start = 8 × group_index
channel_end = min(channel_start + 8, channel_count)

Неполная последняя группа кабеля сохраняется с фактическим count.

Для каждой пары «двухсекундная ячейка × группа восьми каналов» и каждой частоты:

power_sum[k] = сумма всех действительных P[k,c]
count = число вошедших пар frame-channel
power_mean[k] = power_sum[k] / count
power_max[k] = максимальное P[k,c]

Также сохраняются frame_index, channel_index и source_ref, в которых найден power_max.

Все каналы группы и все действительные кадры участвуют в сумме. Выбор одного представительного канала запрещён.

Базовый уровень спектрограммы:

spectral.h5:/levels/t_2s/c_8/spectrum/

Готовая PSD и её характеристики для этой же ячейки:

spectral.h5:/levels/t_2s/c_8/psd/

9.2. Старшие уровни спектрограммы

Постоянные временные уровни spectrum и psd:

t_2s
→ t_10s
→ t_30s
→ t_60s
→ t_300s
→ t_3600s
→ t_21600s
→ t_86400s
→ t_file

Постоянные пространственные уровни:

c_8
→ c_16
→ c_32
→ c_64
→ ...
→ c_all

Для родительской спектральной ячейки:

power_sum_parent[k] = sum(power_sum_child[k])
count_parent = sum(count_child)
power_mean_parent[k] = power_sum_parent[k] / count_parent
power_max_parent[k] = max(power_max_child[k])

Координаты максимума переносятся из победившей дочерней ячейки. Усреднять дочерние power_mean или выбирать каждый N-й кадр и канал запрещено.

Полночастотная группировка по восемь каналов выполняется только один раз при создании базовой спектрограммы. PSD использует те же готовые c_8 ячейки и не выполняет дополнительную группировку.

Интерфейс не обещает восстановление полного спектра отдельного канала. Отдельно для каждого канала сохраняются мощности частотных полос по разделу 11.

10. Спектральная плотность мощности

Отдельное частотное преобразование для PSD не выполняется.

Для каждой готовой спектральной ячейки:

psd_mean[k] = power_sum[k] / count

Если count=0, psd_mean и все характеристики равны NaN.

Пусть:

total_power = sum(psd_mean[k] × delta_f)

Если total_power > 0:

dominant_bin = первый k с максимальным psd_mean[k]
dominant_frequency_hz = frequency_hz[dominant_bin]
dominant_power = psd_mean[dominant_bin]

spectral_centroid_hz =
    sum(frequency_hz[k] × psd_mean[k])
    / sum(psd_mean[k])

spectral_bandwidth_hz =
    sqrt(
        sum(
            (frequency_hz[k] - spectral_centroid_hz)²
            × psd_mean[k]
        )
        / sum(psd_mean[k])
    )

Для энтропии:

q[k] = psd_mean[k] / sum(psd_mean[k])

spectral_entropy =
    -sum(q[k] × ln(q[k])) / ln(F)

Слагаемые с q[k]=0 не учитываются. Энтропия нормирована в диапазон от 0 до 1.

rolloff_95_hz — минимальная frequency_hz[r], для которой:

sum(
    psd_mean[k] × delta_f,
    k=0,...,r
) ≥ 0,95 × total_power

Если total_power=0, все перечисленные характеристики записываются как NaN.

Готовые значения сохраняются в spectral.h5:/levels/<time_level>/<space_level>/psd/ для всех уровней раздела 9.2.

10.1. Децибелы

В HDF5 и ответе FastAPI спектральная плотность и мощности полос передаются только в линейном виде.

Если пользователь включает логарифмическую шкалу, браузер рассчитывает только отображаемое значение:

display_db =
    10 × log10(
        max(linear_power / reference_power, 10^(display_floor_db/10))
    )

По умолчанию:

reference_power = 1 в соответствующих единицах прибора;
display_floor_db = -160 дБ.

Это относительные децибелы относительно единицы прибора, а не дБ SPL. Преобразование не записывается обратно, не участвует в поиске пиков и не меняет сохранённые линейные значения.

11. Мощность частотных полос

Мощности полос вычисляются из P[k,c] до группировки каналов спектрограммы.

Полосы:

[0,20) Гц
[20,80) Гц
[80,250) Гц
[250,500] Гц

Последняя полоса включает частоту Найквиста.

Для каждого кадра, канала и полосы B:

band_power[B,c] =
    sum(P[k,c] × delta_f),
    где frequency_hz[k] принадлежит B

Базовый сохраняемый уровень:

spectral.h5:/levels/t_500ms/c_1/band_power/

То есть сохраняется один результат на каждый канал и каждый шаг кадра 0,5 секунды. Группировка по восемь каналов к band_power не применяется.

Старшие временные уровни band_power:

t_500ms
→ t_2s
→ t_10s
→ t_30s
→ t_60s
→ t_300s
→ t_3600s
→ t_21600s
→ t_86400s
→ t_file

Старшие пространственные уровни:

c_1
→ c_2
→ c_4
→ c_8
→ c_16
→ c_32
→ ...
→ c_all

Для каждого родителя и полосы:

power_sum_parent = sum(power_sum_child)
count_parent = sum(count_child)
power_mean_parent = power_sum_parent / count_parent
power_max_parent = max(power_max_child)

Координаты максимума переносятся из победившей дочерней ячейки. Усреднять дочерние power_mean запрещено.

Хранение результата
12. Что в итоге записывается

Я бы изменил предложенные пять файлов на два основных:

calculated_data/
└── series/
    └── <series_id>/
        └── <file_id>/
            ├── manifest.json
            ├── temporal.h5
            └── spectral.h5

temporal.h5:

базовые статистики по 0,25 с
водопад
активность
временные и пространственные уровни
min/max и координаты пиков

spectral.h5:

общие спектральные мощности
спектрограмма
спектральная плотность
мощности полос
временные и пространственные уровни
максимумы и координаты

Это не означает объединение всей программы в один монолит. Модули расчёта остаются отдельными, но общие результаты физически не дублируются в пяти файлах.

Исходный H5 остаётся первичным источником. Новый файл содержит только подготовленные данные для отображения.

12.1. Точная структура temporal.h5 и spectral.h5

Оба файла используют HDF5 с libver="latest". Порядок групп, названия наборов данных, типы и оси являются частью версии схемы и не могут меняться без увеличения processing_schema_version.

12.1.1. Общий корень файлов

Обязательная структура:

/meta
/sources
/levels

Дополнительно spectral.h5 содержит:

/axes/frequency_hz
/axes/bands_hz

Корневая группа /meta содержит атрибуты:

schema_version — строка;
artifact_type — temporal или spectral;
file_id — строка;
series_id — строка;
source_hash — строка SHA-256;
source_revision — строка;
artifact_revision — uint64;
sample_rate_hz — float64;
channel_spacing_m — float64;
source_start_time_ns — int64;
source_sample_count — int64;
source_channel_count — int32;
fft_window_samples — int32, только для spectral.h5;
fft_hop_samples — int32, только для spectral.h5;
fft_window_name — "hann_periodic", только для spectral.h5;
fft_onesided — uint8 со значением 1, только для spectral.h5;
fft_remove_mean — uint8 со значением 0, только для spectral.h5;
fft_internal_bins_doubled — uint8 со значением 1, только для spectral.h5;
spectrogram_base_seconds — float64 со значением 2,0, только для spectral.h5;
spectrogram_channel_group — int32 со значением 8, только для spectral.h5;
stored_power_scale — "linear", только для spectral.h5;
psd_scaling — "density", только для spectral.h5;
activity_background_seconds — float64 со значением 60,0, только для temporal.h5;
activity_epsilon_absolute — float64 со значением 1e-12, только для temporal.h5;
activity_epsilon_relative — float64 со значением 1.1920929e-7, только для temporal.h5;
created_at_ns — int64;
completed — uint8, завершена ли согласованная первичная публикация;
boundary_complete — uint8, не осталось ли пограничных состояний provisional.

Идентификаторы и SHA внутри HDF5 хранятся как ASCII фиксированной длины S64. Произвольные пути и пользовательские строки остаются только в manifest.json.

Группа /sources содержит таблицу источников, участвующих в координатах результата:

/sources/file_id — S64, форма [N_sources];
/sources/source_hash — S64, форма [N_sources].

source_ref во всех координатах является int32-индексом в этой таблице. Для обычного результата N_sources=1. После пограничного расчёта таблица может содержать соседний файл.

12.1.2. Единая структура уровня

Все многоуровневые данные находятся по пути:

/levels/<time_level>/<space_level>/

Временные уровни имеют фиксированные имена:

t_250ms, t_500ms, t_1s, t_2s, t_5s, t_10s, t_30s, t_60s, t_300s, t_3600s, t_21600s, t_86400s, t_file

Пространственные уровни:

c_1, c_2, c_4, c_8, c_16, c_32, c_64, ... , c_all

Не применимый к конкретному артефакту уровень не создаётся. temporal.h5 начинается с t_250ms. В spectral.h5 группа band_power начинается с t_500ms/c_1, а spectrum и psd — с t_2s/c_8.

Каждая группа уровня содержит:

/axes
/completion

и применимые группы метрик:

/statistics
/waterfall
/activity
/spectrum
/psd
/band_power

Для уровня определяются:

T — число временных ячеек;
S — число пространственных ячеек;
F — число частотных ячеек;
B — число частотных полос.

В /levels/<time_level>/<space_level>/axes хранятся:

time_start_sample — int64, [T];
time_end_sample — int64, [T], правая граница не включается;
time_start_ns — int64, [T];
time_end_ns — int64, [T], правая граница не включается;
time_center_ns — int64, [T];
channel_start — int32, [S];
channel_end — int32, [S], правая граница не включается.

time_start_sample и time_end_sample задаются относительно временной сетки файла-владельца. Для пограничного кадра допускается отрицательный индекс или индекс за концом владельца. Абсолютные time_start_ns и time_end_ns являются нормативными для объединения файлов.

Группа уровня содержит атрибуты:

time_level_name — строка;
space_level_name — строка;
time_bin_seconds — float64;
channels_per_bin — int32, либо -1 для c_all;
axis_order_2d — "time,space";
axis_order_frequency — "time,space,frequency";
axis_order_band — "time,space,band";
level_complete — uint8;
valid_time_bins — uint64.

Каждый набор метрики дополнительно получает атрибут axis_order с фактическим порядком его осей.

12.1.3. Состояние готовности

В /completion хранится отдельный массив состояния для каждой присутствующей группы метрик:

statistics_state — uint8, [T];
waterfall_state — uint8, [T];
activity_state — uint8, [T];
spectrum_state — uint8, [T];
psd_state — uint8, [T];
band_power_state — uint8, [T].

Отсутствующий для уровня массив не создаётся.

Коды состояния:

0 — ячейка ещё не записана;
1 — готова;
2 — готова предварительно и может быть заменена пограничным расчётом;
3 — намеренно отсутствует, например из-за реального пропуска;
4 — ошибка расчёта.

Статус относится ко всей временной строке уровня. Значение 1 устанавливается только после записи всех S ячеек, значений и координат этой строки. FastAPI читает состояния 1 и, с признаком provisional, состояния 2. Состояния 0, 3 и 4 не выдаются как рассчитанные данные.

level_complete=1 устанавливается, когда на уровне не осталось состояний 0 или 2, а все родительские уровни, зависящие от него, записаны. Состояние 3 считается завершённым отсутствием данных, состояние 4 — незавершённой ошибкой.

Корневой completed=1 разрешён при наличии state=2: он означает, что ProcessFileJob атомарно опубликовал согласованный артефакт. boundary_complete остаётся 0 до завершения всех известных BoundaryFinalizeJob. Поэтому processing_status файла и готовность его границ являются независимыми состояниями.

12.1.4. temporal.h5

В каждом применимом уровне temporal.h5 метрики имеют порядок [T,S].

/levels/<time_level>/<space_level>/statistics/:

count — uint64;
missing_count — uint64;
sum — float64;
sumsq — float64;
min — float32;
max — float32;
min_sample_index — int64;
min_channel_index — int32;
min_source_ref — int32;
max_sample_index — int64;
max_channel_index — int32;
max_source_ref — int32.

/levels/<time_level>/<space_level>/waterfall/:

mean — float32;
rms — float32;
energy — float32;
min — float32;
max — float32;
peak_to_peak — float32.

/levels/<time_level>/<space_level>/activity/:

background_count — uint64;
background_mean — float32;
background_std — float32;
rms_activity — float32;
deviation_positive — float32;
deviation_negative — float32;
normalized_deviation — float32;
extremum_value — float32;
extremum_sample_index — int64;
extremum_channel_index — int32;
extremum_source_ref — int32.

Значения min и max намеренно присутствуют и в statistics, и в waterfall. statistics является нормативной основой пирамиды, waterfall — готовым набором для одного запроса графика.

12.1.5. spectral.h5

Частотные оси хранятся один раз:

/axes/frequency_hz — float32, [F];
/axes/bands_hz — float32, [B,2], порядок [нижняя_граница, верхняя_граница].

Границы полос полуоткрытые, кроме последней полосы, включающей частоту Найквиста.

/levels/<time_level>/<space_level>/spectrum/:

power_sum — float64, [T,S,F];
power_mean — float32, [T,S,F];
power_max — float32, [T,S,F];
count — uint64, [T,S];
max_frame_index — int64, [T,S,F];
max_channel_index — int32, [T,S,F];
max_source_ref — int32, [T,S,F].

Для power_sum, power_mean, power_max и координат axis_order="time,space,frequency". Для count axis_order="time,space".

/levels/<time_level>/<space_level>/psd/:

psd_mean — float32, [T,S,F];
dominant_frequency_hz — float32, [T,S];
dominant_power — float32, [T,S];
spectral_centroid_hz — float32, [T,S];
spectral_bandwidth_hz — float32, [T,S];
spectral_entropy — float32, [T,S];
rolloff_95_hz — float32, [T,S].

psd_mean имеет axis_order="time,space,frequency". Остальные показатели имеют axis_order="time,space".

/levels/<time_level>/<space_level>/band_power/:

power_sum — float64, [T,S,B];
power_mean — float32, [T,S,B];
power_max — float32, [T,S,B];
count — uint64, [T,S,B];
max_frame_index — int64, [T,S,B];
max_channel_index — int32, [T,S,B];
max_source_ref — int32, [T,S,B].

Для всех наборов band_power axis_order="time,space,band".

12.1.6. Типы, пустые значения и точность

Обязательные типы:

готовые отображаемые значения — float32;
sum, sumsq и power_sum — float64;
count и missing_count — uint64;
номера отсчётов и кадров — int64;
номера каналов и source_ref — int32;
абсолютное время — int64 в наносекундах UTC;
состояния готовности — uint8.

Все многобайтовые числовые типы записываются явно в little-endian: <f4, <f8, <i8, <i4 и <u8. Использование платформенного native dtype без явного порядка байтов запрещено.

float16, округление до фиксированного числа знаков и scale-offset запрещены.

Значения заполнения до готовности:

float32 и float64 — NaN;
int64 и int32 для неизвестной координаты — -1;
uint64 — 0;
uint8 state — 0.

NaN не заменяется нулём. Действительность ячейки определяется одновременно count и соответствующим state.

12.1.7. Внутренние блоки HDF5

Все расширяемые наборы являются chunked. Размеры внутренних блоков фиксируются так:

двумерные метрики [T,S] — [256, min(S,256)];
частотные метрики [T,S,F] — [16, min(S,16), F];
показатели PSD [T,S] — [256, min(S,256)];
мощности полос [T,S,B] — [128, min(S,128), B];
одномерные временные оси и состояния — [4096];
пространственные оси — один блок длиной S;
frequency_hz — один блок длиной F;
bands_hz — один блок [B,2].

Если фактическое измерение меньше указанного, используется его полный размер. Нулевая временная размерность создаётся с тем же ненулевым размером chunk по времени.

Если необработанный внутренний блок из-за нестандартного F или B превышает 4 МиБ, временная размерность chunk последовательно делится на два, пока блок не станет не больше 4 МиБ. Остальные размеры не меняются.

Для многомерных числовых наборов применяются:

compression="lzf";
shuffle=true;
fletcher32=true.

Для небольших строковых метаданных сжатие не применяется. Потерянное сжатие, quantization и scale-offset запрещены.

12.1.8. Расширение и запись

Временная ось является единственной расширяемой осью:

двумерные наборы создаются с shape=(0,S), maxshape=(None,S);
частотные — shape=(0,S,F), maxshape=(None,S,F);
полосы — shape=(0,S,B), maxshape=(None,S,B);
временные оси и состояния — shape=(0,), maxshape=(None,).

S, F и B после создания уровня не изменяются.

Один пакет записи выполняется в таком порядке:

расширить все наборы уровня до одинакового нового T;
записать временные и пространственные границы;
записать значения;
записать координаты экстремумов;
выполнить flush;
последним действием установить state временных строк;
обновить valid_time_bins.

Родительский уровень строится только из дочерних строк со state=1 или допустимым state=2. Состояния 0, 3 и 4 не участвуют в арифметическом объединении.

Для band_power заранее резервируется строка t_500ms, если центр кадра принадлежит файлу, но окно требует ещё не появившегося соседа. Для spectrum и psd заранее резервируется строка t_2s, если двухсекундная ячейка пересекает такую границу. Эти строки получают state=2. BoundaryFinalizeJob заполняет их либо переводит в state=3. Вставка со сдвигом уже записанных строк запрещена.

12.1.9. Атомарная публикация

ProcessFileJob пишет:

.temporal.h5.<job_id>.tmp
.spectral.h5.<job_id>.tmp

во временные файлы в том же каталоге и на той же файловой системе, что и итоговые temporal.h5 и spectral.h5.

Порядок публикации:

завершить все обязательные уровни;
установить /meta completed=1;
выполнить flush и закрыть HDF5;
выполнить fsync временного файла;
повторно открыть файл только для проверки схемы, размеров, состояний и artifact_revision;
атомарно заменить итоговый файл через os.replace;
выполнить fsync каталога;
только после готовности обоих артефактов атомарно обновить manifest.json и processing_status.

BoundaryFinalizeJob и другие поздние задачи не изменяют опубликованный HDF5 на месте. Они:

получают эксклюзивную блокировку файла .artifact.lock в каталоге результата через fcntl.flock;
создают временную копию текущего HDF5 в том же каталоге;
изменяют только временную копию;
увеличивают artifact_revision;
проверяют её и закрывают;
атомарно заменяют опубликованный файл через os.replace;
атомарно обновляют manifest.json до снятия блокировки.

FastAPI берёт разделяемую блокировку того же .artifact.lock через fcntl.flock на время открытия manifest.json и соответствующих HDF5. Поэтому он видит либо старую, либо новую полностью согласованную версию, но не промежуточную.

После аварии временный файл можно продолжить только при совпадении source_revision, processing_schema_version и job_id. Иначе он удаляется и создаётся заново.

12.1.10. Обязательная проверка перед ready

Артефакт нельзя помечать ready, если не выполнено хотя бы одно условие:

все обязательные пути существуют;
формы наборов внутри уровня согласованы по T, S, F и B;
axis_order соответствует фактической форме;
границы времени и каналов монотонны;
правая граница всегда невключённая;
count не превышает число возможных исходных значений ячейки;
для count=0 отображаемые значения равны NaN, а координаты равны -1;
координаты готового экстремума указывают на существующий source_ref, отсчёт и канал;
строка со state=1 не содержит незаписанных обязательных значений;
completed=1 не имеет обязательных первичных строк со state=0 или 4;
boundary_complete=1 не имеет строк со state=2;
level_complete согласован с состояниями своего уровня;
artifact_revision совпадает с manifest.json.

Паспорт и навигация остаются распределены так:

manifest.json хранит полный паспорт, принадлежность серии, временные отношения, версии и пути к артефактам;
SQLite дублирует только индексируемые поля;
данных, существующих только в SQLite и необходимых для восстановления веб-представления, быть не должно;
отдельные series.json, series_temporal.h5 и series_spectral.h5 не создаются.

12.2. Промежуточные и постоянные значения

Только промежуточными являются:

распакованный блок Phase в 32-битных числах;
остаток неполного окна между блоками;
значения после умножения на окно Ханна;
комплексные коэффициенты частотного преобразования;
полная поканальная P[k,c] отдельного кадра до агрегации;
рабочий буфер фона активности;
накопители, используемые при построении очередного уровня.

Они существуют только во время ProcessFileJob, BoundaryFinalizeJob или OverlapResolverJob и после использования освобождаются.

Постоянно записываются:

все достаточные статистики count, sum, sumsq, min и max вместе с координатами экстремумов;
готовые mean, rms, energy и peak_to_peak для водопада;
готовые фон и показатели активности;
power_sum, power_mean, power_max и count для спектрограммы начиная с t_2s/c_8;
готовая спектральная плотность и её характеристики на тех же c_8 уровнях;
готовые мощности частотных полос начиная с t_500ms/c_1;
паспорт, временные отношения, состояние и версия результата.

Готовые показатели рассчитываются во время предварительной обработки для каждого созданного уровня. FastAPI не выводит mean, rms, отклонения, спектральные характеристики или мощности полос из достаточных статистик во время пользовательского запроса.

Работа веб-интерфейса
13. Выбор участка кабеля

Внутренне ползунок хранит не метры, а номера каналов:

начальный канал = 100
конечный канал = 400

Метры рассчитываются только для подписи:

d=c\cdot\text{meters_per_channel}

Если шаг равен 1,6 м, ползунок физически не может остановиться между каналами.

14. Выбор времени

Ползунок движется с шагом 1 секунда. Это только ограничение интерфейса.

Внутри выбранного интервала остаются:

временные ячейки по 0,25 секунды;
спектральные кадры с шагом 0,5 секунды;
исходные координаты пиков с точностью одного отсчёта, то есть 0,001 секунды.

Пока пользователь двигает ползунки, запрос не выполняется. Он нажимает «Применить», после чего интерфейс отправляет один запрос с готовыми границами.

15. Что делает сервер после «Применить»

Сервер:

Получает время, каналы и размеры графика.
Читает из manifest.json состояния boundaries и признаки provisional.
Выбирает подходящий готовый уровень.
Читает только пересекающиеся блоки temporal.h5 и spectral.h5.
Возвращает подготовленные числовые массивы вместе с boundary_status, provisional_start и provisional_end, если выбранный диапазон их пересекает.
Браузер обновляет графики.

Сервер при этом не должен:

запускать задачу обработки;
читать исходный Phase;
выполнять частотное преобразование;
вычислять или пересчитывать mean, rms, energy и peak_to_peak;
пересчитывать фон или показатели активности;
пересчитывать спектральную плотность и её характеристики;
пересчитывать мощности полос;
читать исходные участки Phase для сравнения наложений;
вычислять контрольные суммы или определять duplicate и conflict;
объединять ячейки арифметически даже на границах выбранного диапазона.

Допускаются только проверка выбора по manifest.json, выбор готового уровня, чтение срезов, объединение последовательных массивов по абсолютному времени, применение уже сохранённой маски владельца наложения и сериализация ответа. Если граница запроса попадает внутрь готовой ячейки, сервер возвращает эту ячейку вместе с её фактическими границами, не создавая новую характеристику.

16. Обработка файлов и построение серий
16.1. Единица обработки и фиксированный размер входного блока

16.1.1. Основное ограничение

Один ProcessFileJob всегда относится ровно к одному физическому H5 и одной неизменной source_revision.

Фиксированная константа:

MAX_DECODED_PHASE_BYTES = 256 × 1024 × 1024 = 268435456 байт

Она ограничивает только расшифрованную матрицу Phase в float32.

Не используются для выбора размера блока:

размер H5 на диске;
коэффициент сжатия HDF5;
размер внутренних HDF5 chunks;
исходный uint16 или float16;
объём свободной памяти сервера;
объём свободной видеопамяти.

Автоматическое увеличение основного блока на мощном сервере запрещено. Изменение этой константы является отдельным изменением конфигурации и не выполняется во время запуска.

16.1.2. Формула количества временных отсчётов

Пусть:

C — полное число каналов Phase;
bytes_per_value = 4;
Q = 500 отсчётов — общий шаг, кратный базовому временному окну 250 и спектральному шагу 500.

Сначала:

rows_raw = floor(MAX_DECODED_PHASE_BYTES / (C × bytes_per_value))

Затем:

если rows_raw ≥ Q, rows_per_block = floor(rows_raw / Q) × Q;
если 1 ≤ rows_raw < Q, rows_per_block = rows_raw;
если rows_raw < 1, файл отклоняется с ошибкой channel_row_exceeds_decoded_limit.

Для каждого полного блока:

decoded_bytes = rows_per_block × C × 4
decoded_bytes ≤ 268435456

Последний блок содержит все оставшиеся временные строки и может быть меньше. Ни один отсчёт не пропускается ради выравнивания.

Пример: физический H5 размером 200 МиБ может потребовать несколько блоков, если после расшифровки Phase превышает 256 МиБ. И наоборот, решение никогда не принимается только по размеру файла на диске.

16.1.3. Ограничение полной рабочей памяти

Основной блок Phase остаётся не больше 256 МиБ, но промежуточные массивы также ограничиваются фиксированными пределами одной задачи:

MAX_CPU_JOB_WORKING_SET_BYTES = 1 × 1024 × 1024 × 1024;
MAX_GPU_JOB_WORKING_SET_BYTES = 1 × 1024 × 1024 × 1024;
WORKING_SET_RESERVE_BYTES = 64 × 1024 × 1024.

Эти пределы являются статической конфигурацией. Программа не опрашивает свободную RAM или VRAM для автоматического увеличения блока.

Перед созданием промежуточного массива задача рассчитывает его полный размер. Одновременно размещённые decoded Phase, копия после перестановки осей, оконный буфер, комплексный результат частотного преобразования, мощности и выходные буферы должны укладываться в соответствующий предел за вычетом WORKING_SET_RESERVE_BYTES.

Если расчёт не укладывается:

основной decoded-блок не увеличивается и не передаётся целиком на GPU;
временная ветвь обрабатывает его последовательными временными поддиапазонами;
частотная ветвь уменьшает число одновременно обрабатываемых кадров и каналов;
результаты подзадач записываются в те же заранее определённые позиции;
математика и итоговые значения от размера подзадачи не меняются.

Если даже один канал одного обязательного окна не помещается в установленный рабочий предел, задача завершается явной ошибкой working_set_limit_exceeded. Продолжать с неполным окном или уменьшать точность запрещено.

16.1.4. Изоляция физических файлов

Один входной decoded-блок ProcessFileJob содержит значения только одного физического файла.

ProcessFileJob хранит неизменяемые:

file_id;
source_path;
source_hash;
source_revision;
result_directory.

Перед каждым чтением и возобновлением проверяется source_revision. Перед каждой записью проверяется, что атрибуты file_id и source_hash группы /meta временного артефакта соответствуют текущей задаче.

Обычный ProcessFileJob имеет N_sources=1 и не может добавлять в /sources соседний файл. Несколько исходных файлов разрешено читать только специализированным BoundaryFinalizeJob или OverlapResolverJob, где каждое значение и экстремум получают явный source_ref. Такие задачи держат срезы источников в разных массивах; суммарный объём одновременно расшифрованной Phase из всех источников также не превышает MAX_DECODED_PHASE_BYTES.

Данные одного файла запрещено:

добавлять в временный буфер другого ProcessFileJob;
записывать в чужой result_directory;
использовать для продолжения незавершённого блока с другим source_revision;
молча объединять с соседним файлом внутри основной обработки.

По умолчанию один вычислительный worker выполняет только один ProcessFileJob одновременно: PROCESS_FILE_CONCURRENCY=1. Увеличение параллелизма допускается только явной конфигурацией очереди и не меняет лимиты отдельной задачи. Каждый job в любом случае сохраняет собственные блоки, хвосты, временные файлы и блокировку каталога.

16.1.5. Перенос состояния между блоками

Граница входного блока не является границей математического окна.

Временная ветвь:

хранит глобальный номер следующего исходного отсчёта;
переносит незавершённый хвост окна 0,25 секунды в следующий блок;
объединяет хвост только со следующим срезом того же file_id и source_revision;
сохраняет последний неполный временной интервал файла с реальным count, не отбрасывая его.

Частотная ветвь:

хранит frame_start следующего ещё не рассчитанного кадра;
после блока сохраняет только хвост, необходимый для следующего окна длиной 1000 отсчётов;
размер хвоста всегда меньше 1000 временных отсчётов;
объединяет хвост только со следующим срезом того же файла;
не вычисляет неполный конечный частотный кадр и не дополняет его нулями.

Все выходные координаты используют глобальные индексы внутри исходного файла, а не локальные индексы блока. Поэтому изменение rows_per_block не меняет координаты, число окон или результат вычислений.

16.1.6. Запись и возобновление

Каждый рассчитанный блок записывается в заранее определённые строки временной оси соответствующих групп /levels/<time_level>/<space_level>/.

Все наборы одного уровня расширяются согласованно по правилам раздела 12.1.8. Произвольное добавление только одного набора «в конец» запрещено.

Во временном артефакте существует служебная группа /checkpoint. После успешной записи блока в её атрибутах сохраняются:

source_revision;
input_start_sample;
input_end_sample;
next_input_sample;
next_temporal_window_start;
next_spectral_frame_start;
temporal_tail_start и temporal_tail_length;
spectral_tail_start и spectral_tail_length;
контрольные суммы хвостов;
состояния /completion/*_state остаются в соответствующих уровнях.

Сами хвосты повторно читаются из того же исходного файла по сохранённым диапазонам; смешивать их с новым файлом запрещено. Перед продолжением их контрольные суммы проверяются.

После сбоя обработка продолжается только при совпадении file_id, source_revision, processing_schema_version и контрольных сумм хвостов. Иначе временный результат удаляется и обработка файла начинается заново. Перед атомарной публикацией служебная группа /checkpoint удаляется.

Лимит 256 МиБ входной Phase не связан с внутренними HDF5 chunks из раздела 12.1.7. Внутренние chunks результата остаются значительно меньше и выбираются исключительно для быстрого чтения веб-запросами.

16.2. Каталог результатов и обнаружение файлов

Единственная нормативная структура calculated_data и полный порядок работы наблюдателя описаны в разделе «WATCHER PRINCIPE» ниже. Другие варианты структуры каталогов не используются.

Наблюдатель создаёт начальный manifest.json до постановки файла в очередь. ProcessFileJob создаёт temporal.h5 и spectral.h5 в каталоге этого физического файла и обновляет состояние и пути в manifest.json.

Если исходный файл позднее изменился, старая обработка инвалидируется.

17. Серия и непрерывный участок

Серия означает, что файлы описывают одинаковую конфигурацию:

одинаковая частота;
одинаковое количество каналов;
одинаковый шаг между каналами;
одинаковое начало и окончание участка кабеля;
одинаковый порядок осей;
одинаковый тип и единица сигнала.

Однако внутри одной серии могут находиться несколько непрерывных временных участков:

Файлы относятся к одной серии, но между участками находится полуторачасовой разрыв.

Для простой пары файлов вычисляется ожидаемое продолжение:

t_expected = t_start_previous + samples_previous / fs

Затем:

Δt = t_start_next - t_expected

Единая классификация для наблюдателя, фоновых задач, FastAPI и интерфейса:

0 ≤ Δt ≤ 1 секунды — логически непрерывное продолжение;
Δt > 1 секунды — временной разрыв и начало нового непрерывного участка;
Δt < 0 — временное наложение, требующее готового решения OverlapResolverJob.

Логически непрерывное продолжение означает, что следующий файл добавляется в ту же временную последовательность без отдельного предупреждения о разрыве. Исходные абсолютные метки времени при этом не изменяются, пропущенные отсчёты не создаются и значения не интерполируются.

Математически точный стык определяется отдельно: первый отсчёт следующего файла должен по сетке дискретизации следовать сразу за последним отсчётом предыдущего. Допустимая погрешность сравнения временных меток не может превышать один период дискретизации, то есть 0,001 секунды при 1000 Гц. Только такой стык разрешает расчёт окна через границу файлов.

Наблюдатель фиксирует только Δt, отношение файлов и границы наложения. Он не сравнивает фазу и не определяет duplicate или conflict.

17.5. Нормативные правила выбора файлов в веб-интерфейсе

Этот раздел является основным контрактом выбора файлов. Наблюдатель, OverlapResolverJob, сортировка серии, manifest.json, FastAPI и веб-интерфейс должны использовать одинаковые правила раздела 17.

FastAPI формирует список серий и файлов из manifest.json каждого обработанного файла. SQLite используется как быстрый индекс путей и порядка, но значения, показываемые пользователю, берутся из manifest.json.

Для каждого файла интерфейс получает из manifest.json:

технический идентификатор файла и серии;
исходное имя файла;
начало, окончание и продолжительность записи;
начало и окончание участка кабеля;
частоту, число каналов и шаг между каналами;
состояние обработки;
идентификатор непрерывного участка;
отношение к предыдущему файлу и Δt;
границы наложения;
состояние разрешения наложения: pending, duplicate, conflict или failed;
источник конфликтного интервала по умолчанию.

Пользователь самостоятельно отмечает файлы внутри выбранной серии.

Правила проверки выбора:

файлы с 0 ≤ Δt ≤ 1 секунды выбираются совместно как одна последовательность; отдельное уведомление о небольшом временном расхождении не показывается;
при Δt < 0 совместный выбор разрешён только после завершения OverlapResolverJob;
duplicate включается в общую временную шкалу один раз без двойного учёта совпадающих значений;
conflict показывается с предупреждением и указанием выбранного источника; пользователь может переключить источник;
pending или failed блокирует совместное применение перекрывающихся файлов и показывает причину;
при Δt > 1 секунды файлы относятся к разным непрерывным участкам и совместное отображение запрещено;
файлы разных серий нельзя включать в один запрос отображения.

Интерфейс не должен молча изменять выбор пользователя. При недопустимой комбинации он объясняет причину и не позволяет применить выбор, пока пользователь её не исправит.

В начале страницы выводится список серий. Для каждой серии показываются участок кабеля и общий временной диапазон крайних файлов, затем непрерывные участки и входящие в них физические файлы.

18. Жизненный цикл пограничных расчётов

Пограничные расчёты не входят в наблюдатель, обычный ProcessFileJob, FastAPI или браузер. Для них используется отдельная фоновая задача BoundaryFinalizeJob.

BoundaryFinalizeJob применяется только к двум файлам одной серии, которые не накладываются по времени и принадлежат одному логически непрерывному участку. Наложения обрабатывает OverlapResolverJob по разделу 21.

18.1. Состояние до появления соседа

ProcessFileJob полностью рассчитывает каждый физический файл независимо и не ждёт будущих соседей.

Если у файла ещё нет готового предыдущего соседа:

внутренние временные и спектральные результаты файла считаются готовыми;
пограничные частотные кадры, которым нужны отсчёты соседнего файла, не создаются;
если предыдущий файл уже известен, но ещё не готов, показатели активности в начале текущего файла помечаются provisional;
если предыдущий файл не известен и файл пока является первым в участке, фон активности считается сброшенным, activity_status получает ready_reset и может быть инвалидирован при позднем появлении более раннего соседа;
для неизвестного будущего следующего файла правая граница получает status: awaiting_neighbor.

Файл можно просматривать до финализации границы. FastAPI возвращает признак provisional только для диапазона, который действительно ожидает известного предыдущего соседа.

18.2. Создание BoundaryFinalizeJob

После изменения порядка файлов наблюдатель записывает отношение соседей в их manifest.json. Сам наблюдатель пограничные вычисления не запускает.

Диспетчер фоновых задач создаёт BoundaryFinalizeJob, когда одновременно выполнены условия:

оба файла имеют processing_status: ready;
файлы принадлежат одной серии;
следующий файл действительно следует за предыдущим в каноническом порядке серии;
0 ≤ Δt ≤ 1 секунды;
для этой пары и текущих версий исходных файлов ещё нет готового результата.

Последовательность:

новый сосед обнаружен
→ оба ProcessFileJob готовы
→ BoundaryFinalizeJob
→ проверка временной сетки
→ финализация активности
→ при точном стыке расчёт пограничных частотных кадров
→ обновление затронутых уровней
→ обновление версий
→ инвалидирование кэша

Если Δt > 1 секунды, BoundaryFinalizeJob не создаётся: фон активности второго файла окончательно считается сброшенным, а граница получает status: skipped_gap.

Если Δt < 0, BoundaryFinalizeJob не создаётся: применяется OverlapResolverJob.

18.3. Проверка математической непрерывности

Логическая непрерывность до одной секунды и математически точный стык являются разными условиями.

Точный стык существует, если первый отсчёт следующего файла по сетке дискретизации непосредственно продолжает предыдущий файл. Погрешность представления временной метки допускается не больше одного периода дискретизации, но пропущенного отсчёта внутри объединяемого окна быть не должно.

BoundaryFinalizeJob записывает:

delta_t_seconds;
is_exact_sample_continuity;
expected_next_sample_time;
actual_next_sample_time.

Если 0 < Δt ≤ 1 секунды и реальный отсчёт отсутствует, файлы остаются одним логическим участком, но частотный кадр через границу не строится и отсутствующее значение не создаётся.

18.4. Пограничные частотные кадры

При точном стыке задача находит только те секундные окна с шагом 0,5 секунды, которые пересекают границу файлов и ещё не были рассчитаны обычными ProcessFileJob.

Для каждого такого окна задача:

читает минимально необходимый хвост Phase предыдущего файла и начало Phase следующего файла отдельными массивами;
при необходимости делит каналы на подзадачи так, чтобы суммарная расшифрованная Phase обоих источников не превышала 256 МиБ, а все промежуточные массивы укладывались в фиксированные рабочие пределы раздела 16.1.3;
применяет строго ту же математику раздела 8, включая периодическое окно Ханна и одностороннее масштабирование;
вычисляет промежуточную P[k,c];
записывает поканальные мощности полос в зарезервированную строку t_500ms/c_1/band_power файла-владельца кадра;
добавляет полночастотный вклад кадра в пересекающую границу базовую ячейку t_2s/c_8/spectrum;
после завершения всей двухсекундной ячейки заново рассчитывает её psd и характеристики;
перестраивает только родительские уровни spectrum, psd и band_power, зависящие от изменённых базовых ячеек.

Правило владения кадром:

frame_center = frame_start + frame_length / 2;
кадр принадлежит файлу, полуоткрытый диапазон [start, end) которого содержит frame_center;
если центр совпадает с start следующего файла, владельцем является следующий файл;
один frame_id может учитываться в band_power только один раз.

Базовая двухсекундная ячейка spectrum/psd принадлежит файлу, диапазон [start,end) которого содержит cell_center. Если cell_center совпадает с началом следующего файла, владельцем является следующий файл.

Если ячейка t_2s пересекает границу, BoundaryFinalizeJob рассчитывает её целиком из всех кадров, центры которых входят в эту ячейку. Частичные версии ячейки из двух файлов не складываются в FastAPI.

В manifest.json владельцев сохраняются frame_id, frame_start, frame_end, frame_center, spectrogram_cell_start, spectrogram_cell_end и идентификаторы обоих исходных файлов.

Если точного стыка нет, spectral_status получает значение skipped_non_continuous, строка пограничного band_power и пересекающая границу spectrum-ячейка получают состояние intentionally_missing, а отсутствующие значения не создаются.

18.5. Восстановление фона активности

Для начала следующего файла BoundaryFinalizeJob не читает исходный Phase повторно. Он использует достаточные статистики из temporal.h5:/levels/t_250ms/c_1/statistics/:

sum;
sumsq;
count;
min;
max.

Задача идёт назад по готовым файлам того же непрерывного участка и набирает до 60 секунд реальной истории. Она останавливается после набора 60 секунд либо в начале непрерывного участка. Ограничиваться только непосредственным соседом нельзя, потому что один физический файл может быть короче 60 секунд.

Затем задача использует начальные окна следующего файла и заново рассчитывает background_mean, background_std, deviation_positive, deviation_negative и normalized_deviation для его начальной части, пока каждому окну не станет доступна требуемая предыдущая история.

При 0 < Δt ≤ 1 секунды фон продолжается через логический стык, но отсутствующий интервал не заполняется: он добавляет ноль к count и ничего к sum и sumsq.

Обновляются только:

temporal.h5:/levels/t_250ms/c_1/activity/ для затронутых окон;
родительские временные и пространственные уровни activity, пересчитанные из statistics по правилам раздела 7.2;
координаты изменившихся экстремумов активности.

Разделы statistics и waterfall не изменяются. После успешного обновления activity_status становится ready, а provisional снимается только с фактически пересчитанного диапазона.

18.6. Что означает обновление уровней серии

Отдельные series_temporal.h5 и series_spectral.h5 по принятой архитектуре не создаются.

Поэтому «обновить уровни серии» означает:

обновить пограничные строки band_power, пересекающие границу ячейки spectrum/psd и только их родительские уровни в spectral.h5 владельцев;
обновить затронутые уровни activity в temporal.h5 следующего файла;
увеличить artifact_revision в manifest.json изменённых файлов;
увеличить series_revision в SQLite;
сделать недействительными кэшированные ответы серии, построенные на предыдущей series_revision.

series_revision является технической версией кэша и индекса. Она не заменяет сведения manifest.json и не является источником пользовательских данных.

Ключ кэша должен включать series_id и series_revision. После увеличения версии старые записи больше не выдаются, а их физическое удаление может выполняться позднее в фоне.

18.7. Надёжность и повторный запуск

BoundaryFinalizeJob имеет уникальный ключ:

series_id + previous_file_id + next_file_id + обе source_revision + background_history_signature + processing_schema_version

Состояния пограничной записи и задачи:

awaiting_neighbor — предыдущий сосед ещё неизвестен;
awaiting_files — отношение известно, но один из файлов ещё не готов;
pending — оба файла готовы и задача поставлена в очередь;
running — задача выполняется;
ready — все требуемые пограничные результаты записаны;
skipped_gap — разрыв больше секунды, расчёт не требуется;
failed — задача завершилась ошибкой.

Перед изменением temporal.h5 или spectral.h5 задача получает блокировку соответствующего артефакта. Запись результатов и всех зависимых уровней завершается до установки status: ready в manifest.json.

Задача должна быть идемпотентной: повторный запуск с тем же ключом не создаёт второй пограничный кадр и не увеличивает статистики повторно.

Пограничный результат инвалидируется и рассчитывается заново, если:

изменился любой исходный H5, участвующий в пограничном кадре или 60-секундной истории;
изменился результат ProcessFileJob одного из участвующих файлов;
изменился состав background_history_signature;
между файлами был вставлен новый сосед;
файл удалён или перемещён в другую серию;
изменилась версия алгоритма или схема хранения.

При ошибке сохраняются status: failed и error_message. Внутренние результаты файлов остаются доступными, но FastAPI возвращает provisional для незавершённой границы и не использует частично записанные пограничные данные.

19. Объединение для веб-интерфейса

Физически склеивать все рассчитанные файлы в один гигантский H5 необязательно.

manifest.json является постоянным источником паспортных данных и временных отношений, которые FastAPI передаёт веб-интерфейсу. SQLite хранит индекс этих паспортов:

путь к manifest.json;
порядок файлов;
абсолютные времена;
идентификаторы непрерывных участков;
разрывы;
перекрытия;
пути к готовым артефактам.

После нажатия «Применить» сервер:

Проверяет выбор по нормативным правилам раздела 17.5.
Определяет, какие файлы пересекают выбранное время.
Читает нужные диапазоны из их готовых temporal.h5 и spectral.h5.
Расставляет данные по абсолютному времени.
При 0 ≤ Δt ≤ 1 секунды последовательно объединяет готовые массивы файлов, не создаёт отдельный объект разрыва и не изменяет временные метки исходных отсчётов.
Если выбор пересекает разрыв больше одной секунды, отклоняет запрос и возвращает описание границы из manifest.json.
Если выбор содержит наложение, использует только готовое решение OverlapResolverJob; при pending или failed совместный запрос отклоняется.

Отдельные series_temporal.h5 и series_spectral.h5 не создаются. Сервер читает готовые уровни из файловых temporal.h5 и spectral.h5, используя порядок и границы из паспортов, проиндексированных в SQLite.

20. Представление в интерфейсе

Интерфейс строится иерархически:

Серия
→ непрерывный участок
→ физические файлы

Состав отображаемых полей, допустимые комбинации файлов, уведомления и ограничения определены разделом 17.5. Интерфейс получает эти сведения через FastAPI из manifest.json и не рассчитывает временные отношения самостоятельно.

21. Фоновое разрешение временных наложений

Сравнение наложений не выполняют наблюдатель, ProcessFileJob, FastAPI или браузер. Для этого существует отдельная фоновая задача OverlapResolverJob.

21.1. Когда создаётся задача

Наблюдатель обнаруживает Δt < 0, вычисляет границы наложения и записывает в manifest.json обоих файлов:

в массиве overlaps обоих паспортов создаётся запись:
status: pending;
other_file_id;
overlap_start;
overlap_end.

Оба физических файла затем обрабатываются полностью. Исходные данные не обрезаются, а temporal.h5 и spectral.h5 создаются независимо для каждого файла.

После перехода обоих файлов в состояние ready диспетчер фоновых задач создаёт один OverlapResolverJob для этой пары и интервала. Сам наблюдатель задачу сравнения не выполняет.

Последовательность:

Watcher
→ зафиксировал overlap со статусом pending
→ оба ProcessFileJob завершены
→ OverlapResolverJob
→ duplicate или conflict
→ обновление manifest.json и SQLite
→ FastAPI читает готовое решение

Если один из исходных файлов изменился или его результат инвалидирован, готовое решение наложения также инвалидируется, status соответствующих записей overlaps возвращается в pending, а после повторной обработки задача запускается заново.

21.2. Как сравнивается наложение

Точный интервал:

overlap_start = max(start_1, start_2)
overlap_end = min(end_1, end_2)

OverlapResolverJob выполняет следующие действия:

проверяет совпадение частоты, числа каналов, порядка осей и сетки временных отсчётов;
переводит границы наложения в диапазоны отсчётов каждого файла;
читает соответствующие части пересечения двумя отдельными массивами;
выбирает размер пары подблоков так, чтобы их суммарная расшифрованная Phase не превышала 256 МиБ, а рабочая память задачи укладывалась в предел раздела 16.1.3;
приводит оба блока к одинаковому порядку time × channel и к тому же каноническому числовому представлению, которое использует основная обработка;
сравнивает маски действительных значений;
сравнивает каждое действительное значение фазы без допуска по амплитуде;
сохраняет контрольные суммы сравниваемых канонических блоков для проверки и воспроизводимости.

Совпадение считается идеальным только тогда, когда временная сетка, маски действительности и все значения фазы для всех каналов во всём пересекающемся интервале совпадают. При несовпадении хотя бы одного значения результат считается conflict. При несовпадении сетки времени результат также считается conflict с причиной time_grid_mismatch.

Контрольная сумма ускоряет повторную проверку, но не заменяет сравнение соответствующих значений. Контрольные суммы самого H5-файла сравнивать недостаточно, потому что вне пересекающегося интервала файлы могут различаться.

21.3. Результат duplicate

duplicate означает совпадение именно пересекающегося интервала, а не обязательную идентичность двух физических файлов целиком.

В канонической временной шкале совпадающий интервал учитывается один раз. Владельцем по умолчанию становится файл, начавшийся раньше; при одинаковом времени начала используется меньший file_id.

Уникальная часть второго файла до или после наложения сохраняется и добавляется в последовательность. Физические файлы и оба комплекта temporal.h5 и spectral.h5 остаются неизменными.

Один повторяющийся граничный отсчёт обрабатывается тем же правилом: если фаза совпала, он берётся один раз и не создаёт предупреждение или видимый разрыв.

21.4. Результат conflict

conflict означает, что пересекающийся интервал содержит разные значения или несовместимую временную сетку.

Автоматически усреднять, складывать, интерполировать или смешивать результаты запрещено. По умолчанию владельцем пересекающегося интервала становится файл, начавшийся раньше; при одинаковом времени начала используется меньший file_id.

Пользователь получает предупреждение и может выбрать второй источник. Переключение передаётся как параметр конкретного запроса и меняет только читаемый готовый temporal.h5 или spectral.h5. Оно не изменяет manifest.json для других пользователей, а исходный H5 повторно не обрабатывается.

Уникальные части обоих файлов вне пересечения остаются доступными. При построении ответа пересекающийся диапазон учитывается только один раз из выбранного источника.

21.5. Что записывается после разрешения

OverlapResolverJob обновляет manifest.json обоих файлов и индекс SQLite:

в соответствующей записи массива overlaps:
status: duplicate или conflict;
other_file_id;
overlap_start;
overlap_end;
default_source_file_id;
comparison_revision;
resolved_at;
comparison_block_hashes;
conflict_reason, если имеется.

Если задача не смогла прочитать или сопоставить интервал, она записывает status: failed и error_message. Такой результат нельзя трактовать как conflict или duplicate.

Временные отношения и готовое решение должны существовать в manifest.json. SQLite только индексирует их для быстрого поиска.

21.6. Поведение FastAPI

FastAPI никогда не читает исходный Phase, не вычисляет контрольные суммы и не сравнивает значения при нажатии «Применить».

FastAPI:

при duplicate читает пересекающийся диапазон только из файла-владельца;
при conflict использует источник из параметра запроса, а при его отсутствии — default_source_file_id, и возвращает предупреждение;
при pending или failed не разрешает совместный запрос перекрывающихся файлов;
вне интервала наложения читает уникальные части каждого выбранного файла;
возвращает overlap_status, границы и фактически использованный источник вместе с данными.

WATCHER PRINCIPE:
1. Назначение наблюдателя

Наблюдатель отвечает только за:

обнаружение H5
→ проверку завершённости копирования
→ чтение паспорта
→ создание технического идентификатора
→ поиск или создание серии
→ сортировку файлов серии
→ определение временного положения
→ запись связей с соседними файлами в manifest.json
→ создание каталогов
→ постановку файла в очередь

Наблюдатель не должен:

рассчитывать графики;
сравнивать значения пересекающихся файлов;
определять, является ли наложение дубликатом;
обрезать файлы;
объединять исходные массивы.

После готовности обоих файлов диспетчер фоновых задач запускает OverlapResolverJob. Только эта задача сравнивает наложение и записывает готовое решение в manifest.json и SQLite.

2. Обнаружение нового файла

Наблюдатель реагирует на появление .h5 и дополнительно пересматривает входную папку примерно каждые 5 секунд.

Файл считается готовым, если:

его размер не изменился за две последовательные проверки;
время изменения не поменялось;
файл успешно открывается;
набор Phase найден;
форма и обязательные метаданные читаются корректно.

Состояния:

discovered
→ stabilizing
→ inspected
→ indexed
→ queued
→ processing
→ ready / failed

Папка calculated_data и временные файлы обязательно исключаются из наблюдения.

3. Паспорт физического файла

Из H5 извлекаются:

исходное имя и путь;
размер;
контрольная сумма;
время начала;
вычисленное время окончания;
частота;
число отсчётов;
число каналов;
шаг между каналами;
начало и окончание участка кабеля;
порядок осей;
тип данных;
идентификатор оборудования или линии, если он присутствует.

Время окончания рассчитывается как:

t
end
	​

=t
start
	​

+
f
s
	​

samples
	​


Внутренне окончание считается невключённой границей.

После сопоставления с серией наблюдатель дополняет manifest.json полями, необходимыми для выбора по разделу 17.5:

file_id;
series_id;
continuous_segment_id;
processing_status;
source_revision;
artifact_revision;
previous_file_id;
relation_to_previous: first, continuous, gap или overlap;
delta_t_seconds;
массив boundaries с neighbor_file_id, side, status, activity_status, spectral_status, provisional_start, provisional_end и job_key;
массив overlaps с other_file_id, overlap_start, overlap_end и status: pending для каждого обнаруженного наложения.

Поля duplicate, conflict, conflict_reason и источник по умолчанию наблюдатель не вычисляет. Их добавляет OverlapResolverJob после обработки обоих файлов.

4. Названия серий и файлов

Время не входит в имя каталога. Оно хранится только в паспорте и SQLite.

Техническое имя серии:

series_0-5000m_a84f19c203bd

Техническое имя результата файла:

file_0-5000m_a23b19e4

Здесь:

0-5000m — участок кабеля;
последние символы — короткий технический идентификатор;
полная контрольная сумма хранится в паспорте.

Метраж в имени нужен для навигации, но истинным идентификатором остаётся контрольная сумма. Поэтому одинаковые участки разных серий или разные файлы одной серии не конфликтуют по именам.

Отображаемое название серии:

Кабель 0–5000 м · 1000 Гц · 3125 каналов

Пользователь может изменить отображаемое название без переименования технической директории.

5. Сопоставление файла с серией

Файл относится к существующей серии, если совпадают:

идентификатор линии или оборудования, если он существует;
начало и окончание участка кабеля;
количество каналов;
шаг между каналами;
частота;
порядок осей;
физический тип данных.

Время записи не участвует в создании серии. Поэтому одна серия может содержать записи одного кабеля за разные часы или дни.

Если идентификатора оборудования нет, для защиты от случайного объединения дополнительно учитывается исходная директория эксперимента.

6. Определение временного отношения файлов

После добавления нового файла все файлы серии сортируются по времени начала, а при одинаковом начале — по file_id.

Наблюдатель проходит список слева направо и поддерживает максимальное время окончания уже покрытого участка. Для нового файла он находит все ранее встреченные файлы, окончание которых позже его времени начала. Каждая такая пара регистрируется как отдельное наложение в массиве overlaps; проверять только соседнюю пару недостаточно.

Если активных наложений нет, Δt вычисляется относительно максимального времени окончания текущего участка по единой формуле и классификации раздела 17.

Наблюдатель выполняет только следующие действия:

при 0 ≤ Δt ≤ 1 секунды помещает файл в текущий непрерывный участок с relation: continuous и создаёт согласованные записи boundaries со status: awaiting_files в manifest.json обоих соседей;
при Δt > 1 секунды начинает новый непрерывный участок с relation: gap и записывает для этой границы status: skipped_gap;
для каждой пересекающейся пары оставляет файлы в одном непрерывном участке и записывает в overlaps status: pending, other_file_id, overlap_start и overlap_end; BoundaryFinalizeJob для такой пары не создаётся.

Наблюдатель не определяет duplicate или conflict и не читает Phase для сравнения значений.

После сортировки он обновляет manifest.json нового файла и всех затронутых предыдущих и следующих соседей. Если канонический сосед изменился, старая запись boundaries инвалидируется до запуска новой задачи. Затем индекс SQLite синхронизируется с паспортами.

Точное положительное значение Δt до одной секунды сохраняется для воспроизводимости, но не передаётся интерфейсу как отдельное предупреждение о разрыве. Исходные временные метки при этом не изменяются.

7. Создание структуры и передача дальше

Результирующая структура:

calculated_data/
├── index.sqlite
└── series/
    ├── series_0-5000m_a84f19c203bd/
    │    ├── file_0-5000m_a23b19e4/
    │    │   ├── manifest.json
    │    │   ├── temporal.h5
    │    │   └── spectral.h5
    │    └── file_0-5000m_b31c287d/
    │        ├── manifest.json
    │        ├── temporal.h5
    │        └── spectral.h5
    │
    └── series_0-2000m_a84f19c203bd/
         ├── file_0-2000m_a23b19e4/
         │   ├── manifest.json
         │   ├── temporal.h5
         │   └── spectral.h5
         └── file_0-2000m_b31c287d/
             ├── manifest.json
             ├── temporal.h5
             └── spectral.h5


После создания паспорта наблюдатель:

Создаёт директорию результата файла.
Записывает начальный manifest.json.
Создаёт или обновляет серию и порядок файлов.
Записывает в manifest.json временные отношения и идентификатор непрерывного участка.
Обновляет manifest.json всех затронутых соседей и инвалидирует устаревшие пограничные записи, если каноническая связь изменилась.
Синхронизирует индекс SQLite с паспортами.
Передаёт идентификатор файла в очередь обработки.

После завершения ProcessFileJob состояние обработки и пути к готовым temporal.h5 и spectral.h5 записываются в manifest.json. Только после этого файл помечается готовым для выбора в интерфейсе. Готовность файла не означает готовность его будущих пограничных расчётов; состояния boundaries ведутся независимо.

Диспетчер фоновых задач находит все пары boundaries со status: awaiting_files, для которых оба файла готовы, переводит их в pending и ставит один BoundaryFinalizeJob на текущие версии пары.

Диспетчер фоновых задач затем находит все записи overlaps со status: pending, для которых оба файла готовы, и ставит один OverlapResolverJob на каждую пару и интервал. Повторная постановка той же версии пары запрещена.
